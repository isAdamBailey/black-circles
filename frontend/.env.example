NUXT_PUBLIC_API_BASE=http://localhost/api/v1

# Optional — GA4 measurement ID. Leave empty to run without analytics. Same
# var name as the root .env (left from the old Vite/vue-gtag setup).
VITE_GOOGLE_TAG_ID=
